{
  "name": "durak",
  "private": true,
  "version": "0.1.0",
  "description": "A web version of Durak (the Russian card game) with heuristic AI bots, move hints, and snarky commentary. Play locally against bots, or online against a friend via a shared room code (Firebase-synced).",
  "workspaces": [
    "packages/engine",
    "packages/client"
  ],
  "scripts": {
    "build:engine": "npm run build -w packages/engine",
    "test": "npm run test -w packages/engine",
    "dev": "npm run build:engine && npm run dev -w packages/client",
    "build": "npm run build:engine && npm run build -w packages/client"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
